import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/contact")({
  component: () => <div className="container-page py-20 text-center text-muted-foreground">Contact — coming in Phase 3</div>,
});
